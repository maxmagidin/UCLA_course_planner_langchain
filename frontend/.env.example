# Leave blank when FastAPI serves the frontend or Vite proxies /api locally.
VITE_API_BASE_URL=

# Set this only for static hosting under a repository path.
# VITE_BASE_PATH=/UCLA_course_planner_langchain/
